<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for Bazaar.

  SECURITY (Finding 7): On Android 9+ (API 28+) cleartext HTTP is blocked by
  default. The Ollama provider needs to talk to a local Ollama server over
  plain HTTP (http://localhost:11434). This config whitelists cleartext for
  localhost, 127.0.0.1, and 10.0.2.2 (the Android emulator's host-loopback)
  only. All other hosts continue to require HTTPS.

  Reference: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <!--
      Localhost / loopback / emulator-host: cleartext allowed for the Ollama
      self-hosted LLM provider. These addresses never leave the device.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

    <!--
      Everything else: default Android behaviour (HTTPS required on API 28+).
      No cleartext for searxng / openfoodfacts / huggingface / LLM provider
      APIs.
    -->
</network-security-config>
