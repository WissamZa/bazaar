# This file is a placeholder so the assets/fonts/ directory exists.
# Download the real NotoSansArabic TTFs from
# https://fonts.google.com/noto/specimen/Noto+Sans+Arabic
# and replace these placeholders:
#   NotoSansArabic-Regular.ttf
#   NotoSansArabic-Bold.ttf
# Without these the app still runs (Flutter falls back to the system
# Arabic font) but the new ﷼ symbol may not render on older devices.
